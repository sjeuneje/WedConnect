export default function DashboardLayout({ children }) {
    return (
        <div className="ml-[250px] mt-[75px] h-[calc(100vh-100px)] overflow-auto p-4 bg-[#FDFDFC]">
            {children}
        </div>
    )
}
